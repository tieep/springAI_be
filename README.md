# spring-ai-image-analysis-cookbook
A Spring AI "cookbook" demonstrating how we can build image analysis API. This service uses an LLM to interpret images based on images provided in multiple formats: direct file uploads (multipart), web URLs, classpath resources, and Base64 strings.
